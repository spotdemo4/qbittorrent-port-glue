#:schema https://www.schemastore.org/treefmt.json

[formatter.ruff]
command = "ruff"
options = ["format"]
includes = ["*.py"]

[formatter.nixfmt]
command = "nixfmt"
includes = ["*.nix"]

[formatter.oxfmt]
command = "oxfmt"
options = ["--write"]
includes = ["*.json", "*.yaml", "*.toml", "*.md"]
